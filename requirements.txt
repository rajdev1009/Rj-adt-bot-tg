# ── Core ──────────────────────────────────────────────────────
pyrogram==2.0.106
TgCrypto==1.2.5           # Required C extension for Pyrogram (speed + security)

# ── Database ──────────────────────────────────────────────────
motor==3.3.2              # Async MongoDB driver (wraps pymongo)
pymongo==4.6.1            # MongoDB sync driver (motor dependency)
asyncpg==0.29.0           # Async PostgreSQL driver (NeonDB)

# ── HTTP ──────────────────────────────────────────────────────
aiohttp==3.9.3            # Async HTTP client (link shortener API calls)

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1      # Load .env files in development
pytz                      # Timezone support (token verification)
